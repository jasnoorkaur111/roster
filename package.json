{
  "name": "roster",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local web app: pulls your Luma events, profiles every guest, and flags the top 10 people to talk to.",
  "scripts": {
    "dev": "node --watch src/server.mjs",
    "start": "node src/server.mjs"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "zod": "^4.6.5"
  }
}
